# CPU-only PyTorch wheel — sentence-transformers and the DeBERTa NLI model
# both pull in torch. Without this, `pip install torch` on Streamlit Cloud's
# Linux host pulls the CUDA-bundled build (multi-GB) instead of the ~200MB
# CPU build, which blows the platform's build size/memory limits.
--extra-index-url https://download.pytorch.org/whl/cpu

# Ingestion + retrieval orchestration (Parts 1, 3, 4)
langchain==1.3.13
langchain-community==0.4.2
langchain-groq==1.1.3
pypdf==6.14.2

# Embeddings (Part 2)
sentence-transformers==5.6.0

# Vector store (Part 3)
faiss-cpu==1.14.3

# Generation — Llama 3 via Groq (Part 4)
groq==0.37.1

# Verification — DeBERTa NLI (Part 5)
transformers==5.13.1
torch==2.13.0

# Frontend (Part 6)
streamlit==1.59.2

# Secrets / config
python-dotenv==1.2.2
